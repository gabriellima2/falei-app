import { View } from "react-native";
import { Header, Typography } from "@/ui/components";

export default function Page() {
	return (
		<View>
			<Header title="Sua trajetória" />
			<Typography.Title>Analytics</Typography.Title>
		</View>
	);
}
